---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-03-21T12:44:08.739Z
provider: 
---

# 📐 记忆治理与知识复利规则（Memory Governance & Knowledge Compounding）

> **生效范围**：所有会话
> **版本**：v1.0
> **创建**：2026-03-21

---

## 一、记忆隔离协议（MUST FOLLOW）

### 1.1 四域分离

系统包含四个独立的记忆域，**严禁跨域污染**：

| 域 | 路径 | 用途 | 隔离规则 |
|----|------|------|---------|
| 项目域 | `.codebuddy/memory/projects/{slug}/` | 项目特定信息 | 不同项目的 slug 目录完全隔离 |
| 规则域 | `.codebuddy/rules/` | 可执行指令 | 按触发条件加载，不主动加载无关规则 |
| 经验域 | `.codebuddy/memory/knowledge/` | 跨项目通用经验 | 只存去项目化的通用知识 |
| 偏好域 | `.codebuddy/MEMORY.md` | 用户偏好 | 每次会话自动加载 |

### 1.2 隔离规则

- **项目域互不访问**：处理项目 A 时，不得读取项目 B 的 `state.md`
- **经验域去项目化**：写入 `knowledge/` 前必须移除项目名称、具体数值等项目特定信息
- **偏好域只读**：除用户明确要求修改外，不得自动修改 `MEMORY.md` 的偏好部分
- **规则域按需加载**：只在触发词匹配或 `alwaysApply: true` 时加载

### 1.3 日志标签化

写入日志 `memory/YYYY-MM-DD.md` 时，每个条目必须标注所属项目：
```markdown
## [multi-agent-framework] 新增专利申请团队
- 内容...

## [wiz-migration] 修复重复标题
- 内容...
```

---

## 二、知识复利协议（SHOULD FOLLOW）

### 2.1 使用追踪

每次使用 `knowledge/` 或 `rules/` 中的知识/规则后，追加记录到 `knowledge/_usage_log.md`：

```markdown
- [USED] {资源路径} → {用途}
  - 使用效果：{成功/部分成功/失败}
  - 改进信号：{发现的不足}
  - 状态：{✅ 已反馈 | ⏳ 待处理}
```

### 2.2 蒸馏触发

满足以下任一条件时，应提议将经验蒸馏到经验域：

1. **频率阈值**：同类经验在 `_usage_log.md` 中出现 ≥ 3 次
2. **用户确认**：用户明确表示某经验值得沉淀
3. **失败教训**：某操作失败且有通用的避坑价值

### 2.3 蒸馏流程

```
识别候选 → 去项目化 → 分类（patterns/ 或 lessons/）→ 写入 → 更新 _index.md → 记录 _evolution_log.md
```

### 2.4 进化协议

当规则/框架被改进时：
1. 在 `_evolution_log.md` 记录变更（使用 [EVO-NNN] 编号）
2. 更新对应文件的版本信息
3. 更新 `_usage_log.md` 中对应条目的状态

---

## 三、公共记忆边界（MUST FOLLOW）

### 3.1 允许进入经验域

- ✅ 通用设计模式（如「贯穿型 Agent ≤ 5 个」）
- ✅ 工具使用最佳实践（如「大文件用 replace_in_file」）
- ✅ 错误教训（如「重复标题会导致覆盖」）
- ✅ 质量检查清单
- ✅ 架构决策原则

### 3.2 禁止进入经验域

- ❌ 项目特定数据（ROI 数值、客户名称）
- ❌ 敏感信息（密钥、密码、Token）
- ❌ 临时状态（调试结果、搜索缓存）
- ❌ 个人隐私
- ❌ 大段原始文件内容

---

## 四、项目域生命周期

### 4.1 创建

当识别到新项目（用户首次提及且涉及多步骤工作）时：
```
.codebuddy/memory/projects/{slug}/state.md
```

### 4.2 更新

项目域的 `state.md` 在以下时机更新：
- 关键决策做出后
- 里程碑完成后
- 项目状态变更后

### 4.3 归档

项目完成或 30 天未活跃：
- 在 `MEMORY.md` 的项目索引中标记为 ✅ 完成
- 项目域文件保留不删除，但不再主动加载

---

## 五、会话启动检查清单

每次新会话开始时：
1. ✅ 加载 `MEMORY.md`（偏好域）
2. ✅ 加载当日 + 昨日日志（如有）
3. ⏸️ 项目域：等待用户提及相关项目后再加载
4. ⏸️ 经验域：等待相关场景出现后再推荐
5. ⏸️ 规则域：等待触发条件匹配