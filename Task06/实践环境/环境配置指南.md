# 第13章实践环境配置指南

> 本指南帮助您从零开始搭建智能旅行助手的开发和运行环境

## 📋 环境检查清单

在开始之前，请确认您的系统满足以下要求：

- [ ] Python 3.10 或更高版本
- [ ] Node.js 16 或更高版本
- [ ] Git (已完成 ✅)
- [ ] 文本编辑器 (VS Code 推荐)
- [ ] 稳定的网络连接（用于 API 调用）

### 检查现有环境

```powershell
# 检查 Python 版本
python --version

# 检查 Node.js 版本
node --version

# 检查 npm 版本
npm --version

# 检查 Git 版本
git --version
```

---

## 🔑 API 密钥准备

### 1. 高德地图 API 密钥 ⭐⭐⭐ (必需)

高德地图是本项目的核心依赖，用于景点搜索、路线规划和天气查询。

#### 申请步骤：

1. **注册账号**
   - 访问：https://lbs.amap.com/
   - 点击"注册"，使用手机号注册账号

2. **创建应用**
   - 登录后进入"控制台"
   - 点击"应用管理" → "我的应用" → "创建新应用"
   - 填写应用名称：`HelloAgents旅行助手学习项目`
   - 应用类型：选择"Web服务"

3. **获取 API Key**
   - 在应用下点击"添加key"
   - 服务平台：选择 **"Web服务"**（重要！）
   - 填写 Key 名称：`学习环境Key`
   - 提交后即可获得 API Key

4. **配置使用额度**
   - 免费额度：每日 30万次调用
   - 开发学习完全够用

**需要两个 Key**：
- **Web服务 API Key**：后端使用（通过 MCP 调用）
- **Web端 JavaScript API Key**：前端地图显示使用

> 💡 **重要提示**：
> - Web服务 Key 和 JavaScript Key 是不同的，需要分别申请
> - 保管好您的 API Key，不要提交到公共代码仓库

---

### 2. LLM API 密钥 ⭐⭐⭐ (必需)

项目支持多种 LLM 提供商，推荐以下选项：

#### 选项 A: DeepSeek (推荐新手)
- **优点**：价格便宜、速度快、效果好
- **价格**：¥1/百万 tokens（输入），¥2/百万 tokens（输出）
- **申请地址**：https://platform.deepseek.com/
- **文档**：https://platform.deepseek.com/docs

**申请步骤**：
1. 访问 DeepSeek 开放平台
2. 注册并登录
3. 充值（最低 ¥10）
4. 在"API Keys"页面创建新的 API Key

**配置示例**：
```
LLM_MODEL_ID=deepseek-chat
LLM_API_KEY=sk-xxxxxxxxxxxxx
LLM_BASE_URL=https://api.deepseek.com/v1
```

#### 选项 B: OpenAI
- **优点**：性能最强
- **缺点**：价格较高，国内访问需要代理
- **申请地址**：https://platform.openai.com/

**配置示例**：
```
LLM_MODEL_ID=gpt-4o-mini
LLM_API_KEY=sk-proj-xxxxxxxxxxxxx
LLM_BASE_URL=https://api.openai.com/v1
```

#### 选项 C: 阿里云百炼（通义千问）
- **优点**：国内访问稳定
- **申请地址**：https://bailian.console.aliyun.com/

---

### 3. Unsplash API 密钥 ⭐ (可选)

用于获取景点的高质量图片，不是必需的。

#### 申请步骤：
1. 访问：https://unsplash.com/developers
2. 注册账号并创建应用
3. 获取 Access Key 和 Secret Key

**免费额度**：每小时 50 次请求

---

## 🐍 后端环境配置

### 1. 创建 Python 虚拟环境

```powershell
# 进入后端目录
cd C:\Users\frankechen\CFP-Study\Task06\official-code\code\chapter13\helloagents-trip-planner\backend

# 创建虚拟环境
python -m venv venv

# 激活虚拟环境（Windows PowerShell）
.\venv\Scripts\Activate.ps1

# 如果遇到权限问题，运行以下命令后重试：
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### 2. 安装 Python 依赖

```powershell
# 确保虚拟环境已激活（命令行前应显示 (venv)）

# 升级 pip
python -m pip install --upgrade pip

# 安装项目依赖
pip install -r requirements.txt
```

**依赖列表**：
- `hello-agents[protocols]>=0.2.4` - HelloAgents 框架（核心）
- `fastapi>=0.115.0` - Web 框架
- `uvicorn[standard]>=0.32.0` - ASGI 服务器
- `pydantic>=2.0.0` - 数据验证
- `httpx>=0.27.0` - HTTP 客户端
- `python-dotenv>=1.0.0` - 环境变量管理
- `fastmcp>=2.0.0` - MCP 协议支持
- `uv>=0.8.0` - Python 包管理工具（用于运行 MCP 服务器）

### 3. 配置环境变量

```powershell
# 复制示例配置文件
Copy-Item .env.example .env

# 使用记事本或 VS Code 编辑 .env 文件
notepad .env
# 或
code .env
```

**编辑 `.env` 文件内容**：

```env
# ============================================
# LLM 配置（必需）
# ============================================
LLM_MODEL_ID=deepseek-chat
LLM_API_KEY=sk-your-deepseek-api-key-here
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_TIMEOUT=60

# ============================================
# 高德地图 API 配置（必需）
# ============================================
AMAP_API_KEY=your-amap-web-service-key-here

# ============================================
# Unsplash API 配置（可选）
# ============================================
UNSPLASH_ACCESS_KEY=your-unsplash-access-key-here
UNSPLASH_SECRET_KEY=your-unsplash-secret-key-here

# ============================================
# 服务器配置
# ============================================
HOST=0.0.0.0
PORT=8000

# ============================================
# CORS 配置（允许前端跨域访问）
# ============================================
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# ============================================
# 日志级别
# ============================================
LOG_LEVEL=INFO
```

### 4. 安装 MCP 服务器

HelloAgents 使用 MCP (Model Context Protocol) 来集成高德地图服务。

```powershell
# 确保已安装 uv（应该已通过 requirements.txt 安装）
uv --version

# 测试 amap-mcp-server 是否可用
uvx amap-mcp-server --help
```

> **注意**：`amap-mcp-server` 不需要单独安装，HelloAgents 会在运行时自动通过 `uvx` 启动。

---

## 🎨 前端环境配置

### 1. 安装 Node.js 依赖

```powershell
# 进入前端目录
cd C:\Users\frankechen\CFP-Study\Task06\official-code\code\chapter13\helloagents-trip-planner\frontend

# 安装依赖
npm install

# 如果 npm 速度慢，可以使用国内镜像
npm config set registry https://registry.npmmirror.com
npm install
```

**依赖列表**：
- `vue@^3.5.13` - Vue 3 框架
- `vue-router@^4.5.0` - 路由管理
- `ant-design-vue@^4.2.6` - UI 组件库
- `axios@^1.7.9` - HTTP 客户端
- `@amap/amap-jsapi-loader@^1.0.1` - 高德地图加载器
- `typescript@^5.7.3` - TypeScript 支持
- `vite@^6.0.7` - 构建工具

### 2. 配置前端环境变量

```powershell
# 创建 .env 文件
New-Item -Path .env -ItemType File

# 编辑文件
notepad .env
# 或
code .env
```

**编辑 `.env` 文件内容**：

```env
# 高德地图 Web 端 JavaScript API Key（必需）
VITE_AMAP_WEB_KEY=your-amap-javascript-api-key-here

# 后端 API 地址（默认）
VITE_API_BASE_URL=http://localhost:8000
```

---

## ✅ 环境验证

### 1. 验证后端环境

```powershell
# 进入后端目录
cd C:\Users\frankechen\CFP-Study\Task06\official-code\code\chapter13\helloagents-trip-planner\backend

# 激活虚拟环境
.\venv\Scripts\Activate.ps1

# 启动后端服务
uvicorn app.api.main:app --reload --host 0.0.0.0 --port 8000
```

**预期输出**：
```
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
INFO:     Started reloader process [xxxxx]
INFO:     Started server process [xxxxx]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
```

**验证步骤**：
1. 打开浏览器访问：http://localhost:8000/docs
2. 应该看到 FastAPI 自动生成的 API 文档页面
3. 尝试调用 `/health` 端点测试服务是否正常

### 2. 验证前端环境

**打开新的 PowerShell 窗口**：

```powershell
# 进入前端目录
cd C:\Users\frankechen\CFP-Study\Task06\official-code\code\chapter13\helloagents-trip-planner\frontend

# 启动开发服务器
npm run dev
```

**预期输出**：
```
VITE v6.0.7  ready in xxx ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
➜  press h + enter to show help
```

**验证步骤**：
1. 打开浏览器访问：http://localhost:5173
2. 应该看到旅行规划助手的首页
3. 检查浏览器控制台是否有错误

---

## 🚀 完整启动流程

### 启动顺序

1. **先启动后端** (端口 8000)
2. **再启动前端** (端口 5173)

### Windows 快速启动脚本

我将在下一步为您创建自动化启动脚本！

---

## 🐛 常见问题排查

### 1. Python 虚拟环境激活失败

**错误信息**：
```
无法加载文件 venv\Scripts\Activate.ps1，因为在此系统上禁止运行脚本
```

**解决方案**：
```powershell
# 临时允许脚本执行
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# 再次尝试激活
.\venv\Scripts\Activate.ps1
```

### 2. pip 安装依赖失败

**常见原因**：
- 网络问题
- Python 版本不兼容

**解决方案**：
```powershell
# 使用国内镜像
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 如果单个包失败，单独安装
pip install hello-agents[protocols] -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 3. npm 安装依赖慢或失败

**解决方案**：
```powershell
# 使用国内镜像
npm config set registry https://registry.npmmirror.com

# 清除缓存后重试
npm cache clean --force
npm install
```

### 4. uvx 命令找不到

**错误信息**：
```
uvx : 无法将"uvx"项识别为 cmdlet、函数、脚本文件或可运行程序的名称
```

**解决方案**：
```powershell
# 重新安装 uv
pip install --upgrade uv

# 或使用 pipx 安装（推荐）
pip install pipx
pipx install uv
pipx ensurepath

# 重启 PowerShell 使环境变量生效
```

### 5. FastAPI 启动报错：找不到模块

**错误信息**：
```
ModuleNotFoundError: No module named 'app'
```

**解决方案**：
```powershell
# 确保在 backend 目录下运行
cd C:\Users\frankechen\CFP-Study\Task06\official-code\code\chapter13\helloagents-trip-planner\backend

# 使用正确的启动命令
uvicorn app.api.main:app --reload
```

### 6. MCP 工具调用失败

**错误信息**：
```
Failed to connect to MCP server
```

**排查步骤**：
1. 检查 `.env` 文件中的 `AMAP_API_KEY` 是否正确
2. 确认 `uv` 和 `uvx` 命令可用
3. 检查网络连接
4. 查看后端日志中的详细错误信息

### 7. 高德地图前端显示不出来

**可能原因**：
- JavaScript API Key 错误或未配置
- Key 的安全配置限制了访问域名

**解决方案**：
1. 确认 `.env` 文件中的 `VITE_AMAP_WEB_KEY` 已配置
2. 在高德开放平台控制台检查 Key 的绑定域名设置
3. 开发环境可以将域名设置为 `localhost`

---

## 📚 下一步

环境配置完成后，您可以：

1. ✅ 运行官方代码，体验完整功能
2. ✅ 阅读源码，理解实现原理
3. ✅ 参考学习指南，逐步学习每个模块
4. ✅ 尝试修改代码，添加新功能

**相关文档**：
- [第13章官方学习指南](../学习笔记/第13章-智能旅行助手-官方学习指南.md)
- [官方文档](https://github.com/datawhalechina/hello-agents/blob/main/docs/chapter13/第十三章%20智能旅行助手.md)
- [快速启动脚本](./快速启动脚本.md)（即将创建）

---

## 💡 学习建议

1. **先理解再实践**：在修改代码前，先理解每个组件的作用
2. **逐步调试**：从简单的 API 调用开始测试
3. **查看日志**：遇到问题时，后端的日志信息非常有用
4. **参考文档**：善用 FastAPI 文档（http://localhost:8000/docs）
5. **循序渐进**：不要急于添加新功能，先把基础功能跑通

---

**祝您学习愉快！** 🎉

如遇到问题，请查看：
- 官方 Issues: https://github.com/datawhalechina/hello-agents/issues
- 学习笔记中的常见问题章节
